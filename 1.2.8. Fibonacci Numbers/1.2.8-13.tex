\documentclass{article}
\usepackage{amsmath, amssymb}

\title{1.2.8-13}

\begin{document}
\maketitle

\section*{a)}

\( a_0 = r, a_1=s, a_{n+2} = a_{n+1} + a_n \)

\( F_0 = 0, F_1=1, F_{-1}=1, F_{n+2} = F_{n+1} + F_n \)

\( b_n := a_n - rF_{n-1} - sF_{n} \)

\( b_0 = a_0 - rF_{-1} - s F_0 = r - r \cdot 1 - s \cdot 0 = 0 \)

\( b_1 = a_1 - rF_0 - s F_1 = s - r \cdot 0 - s \cdot 1 = 0 \)

\( \forall n \geq 2: b_n = a_n - rF_{n-1} - sF_{n} = \)

\( = a_{n-1} + a_{n-2} - r (F_{n-2} + F_{n-3}) - s (F_{n-1} + F_{n-2}) = (a_{n-1} - rF_{n-2} - sF_{n-1}) + \)

\( + (a_{n-2} - rF_{n-3} - sF_{n-2})  = b_{n-1} + b_{n-2} \)

By induction, \( \forall n: b_n = 0 \implies a_n = rF_{n-1} + s F_n \)

\section*{b)}

\( b_0 = 0, b_1 = 1, b_{n+2} = b_{n+1} + b_n + c \)

\( c_n := b_n + c \)

\( c_0 = c \)

\( c_1 = 1 + c \)

\( c_{n + 2} = b_{n+2} + c = b_{n+1} + b_{n} + c + c= (b_{n+1} + c) + (b_{n} + c) = c_{n+1} + c_n \)

From a)

\( c_n = cF_{n-1} + (1+c)F_n \)

\( b_n = -c + cF_{n-1} + (1+c)F_n = -c + c(F_{n-1}+F_n) + F_n = -c + F_n + cF_{n+1} \)

\section*{a) version 2}

\( A(z) = \sum_{n \geq 0} a_n z^n = r + sz + \sum_{n \geq 2} a_n z^{n} = r + sz + \sum_{n \geq 0} a_{n+2}z^{n+2} = \)

\( = r + sz + \sum_{n \geq 0} (a_{n+1} + a_n) z^{n+2} = r + sz + z \sum_{n \geq 0} a_{n+1} z^{n+1} + z^2 \sum_{n \geq 0} a_n z^{n} = \)

\( = r + sz + z \sum_{n \geq 1}a_n z^n + z^2 A(z) = r + sz + z (A(z)-r) + z^2 A(z) = \)

\( = r + (s-r)z + A(z)(z+z^2) \)

\( A(z) = \frac{r + (s-r)z}{1-z-z^2} \)

\( F(z) = \sum_{n \geq 0} F_nz^n = \frac{z}{1-z-z^2} \)

\( A(z) = \frac{r+(s-r)z}{z}F(z) = (s-r)F(z) + r \frac{1}{z}F(z) \)

\( a_n = (s-r)F_{n} + r F_{n+1} = s F_n + r(F_{n+1} - F_{n}) = s F_n + r F_{n-1} \)

\section*{b) version 2}

\( B(z) = \sum_{z \geq 0} b_nz^n = z + \sum_{n \geq 2} b_{n+2}z^{n+2} = z + \sum_{n \geq 0} (b_{n+1} + b_n + c)z^{n+2} = \)

\( = z + z\sum_{n \geq 0} b_{n+1}z^{n+1} + z^2 \sum_{n\geq 0}b_nz^n + \frac{cz^2}{1-z} = z + z(B(z) - 0) + z^2 B(z) + \frac{cz^2}{1-z} \)

\( B(z) = \frac{z + \frac{cz^2}{1-z}}{1-z-z^2} = F(z)(1 + \frac{cz}{1-z}) = F(z) + F(z) z\frac{1}{1-z} \)

\( b_n = F_n + c[z^n]F(z)z\frac{1}{1-z} = F_n + c[z^{n-1}]F(z)\frac{1}{1-z} = F_n + c\sum_{k=0}^{n-1}F_k \)

\( F_k = F_{k+2} - F_{k+1} \)

\( \sum_{k=0}^{n-1}F_k = \sum_{k=0}^{n-1}(F_{k+2} - F_{k+1}) = F_{n+1} - F_1 = F_{n+1} - 1 \)

\( b_n = F_n + c(F_{n+1} - 1) = -c + F_n + cF_{n+1} \)

\section*{a) version 3}

\( x_0 = 0, x_1 = 1, x_{n+2} = x_{n+1} + x_n \implies x_n = F_n \)

\( x_1 = 1, x_1 = 0, x_{n+2} = x_{n+1} + x_n \implies x_n = F_{n-1} \)

\( a_n = rF_{n-1} + sF_{n} \)


\end{document}
