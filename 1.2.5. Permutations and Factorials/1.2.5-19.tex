\documentclass{article}
\usepackage{amsmath, amssymb}

\title{1.2.5-19}

\begin{document}
\maketitle

\( \Gamma_m(x) = \frac{m^x m!}{x(x+1)\cdot\quad\cdots\quad\cdot(x+m)} \)

\( A_m(x) := \int_0^m (1-\frac{t}{m})^m t^{x-1}dt \overset{t=ms}{=} \int_0^1 (1-s)^m(ms)^{x-1}mds = m^x \overbrace{\int_0^1 (1-s)^ms^{x-1}ds}^{B_m(x)} \)

\( C_m(x) := \frac{\Gamma_m(x)}{m^x} = \frac{m!}{x(x+1)\cdot\quad\cdots\quad\cdot(x+m)} \)

Let's prove by induction of \( m: B_m(x) = C_m(x) \)

\( m = 1: B_1(x) = \int_0^1 (1-s)^1 s^{x-1}ds = \int_0^1 (s^{x-1}-s^x)ds = \frac{1}{x} - \frac{1}{x+1} = \frac{1}{x(x+1)} = C_1(x) \)

\( (m \implies m + 1) \)

\( B_{m+1}(x) = \int_0^1 (1-s)^{m+1}s^{x-1}ds = \frac{1}{x} \int_0^1 (1-s)^{m+1}d(s^{x}) = \frac{1}{x}(1-s)^{m+1}s^x \Big|_0^1 - \frac{1}{x} (m+1)(-1) \int_0^1 (1-s)^{m}s^{x}ds = \)

\( = \frac{m+1}{x} B_m(x+1) = \frac{m+1}{x} C_m(x+1) = C_{m+1}(x) \)


\end{document}
