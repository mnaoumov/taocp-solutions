\documentclass{article}
\input{../../template} % chktex 27

\title{1-2} % chktex 8

\begin{document}
\maketitle

if \( E1 \) is not the first time appeared. Then it is came after step \( E3 \).\ \( m \leftarrow n, n \leftarrow r \).

But as \( r \) is a remainder of deletion, \( r < n \). Then after reassignment \( n < m \).

\textbf{Q.E.D.}


\end{document}
