\documentclass{article}
\input{../../../template} % chktex 27

\title{1.2.8-13} % chktex 8

\begin{document}
\maketitle

\section*{a)} % chktex 9 % chktex 10

\( a_{0} = r, a_{1}=s, a_{n+2} = a_{n+1} + a_{n} \)

\( F_{0} = 0, F_{1}=1, F_{-1}=1, F_{n+2} = F_{n+1} + F_{n} \)

\( b_{n} := a_{n} - rF_{n-1} - sF_{n} \)

\( b_{0} = a_{0} - rF_{-1} - s F_{0} = r - r \cdot 1 - s \cdot 0 = 0 \)

\( b_{1} = a_{1} - rF_{0} - s F_{1} = s - r \cdot 0 - s \cdot 1 = 0 \)

\( \forall n \geq 2: b_{n} = a_{n} - rF_{n-1} - sF_{n} = \)

\( = a_{n-1} + a_{n-2} - r (F_{n-2} + F_{n-3}) - s (F_{n-1} + F_{n-2}) = (a_{n-1} - rF_{n-2} - sF_{n-1}) + \)

\( + (a_{n-2} - rF_{n-3} - sF_{n-2})  = b_{n-1} + b_{n-2} \)

By induction, \( \forall n: b_{n} = 0 \implies a_{n} = rF_{n-1} + s F_{n} \)

\section*{b)} % chktex 9 % chktex 10

\( b_{0} = 0, b_{1} = 1, b_{n+2} = b_{n+1} + b_{n} + c \)

\( c_{n} := b_{n} + c \)

\( c_{0} = c \)

\( c_{1} = 1 + c \)

\( c_{n + 2} = b_{n+2} + c = b_{n+1} + b_{n} + c + c= (b_{n+1} + c) + (b_{n} + c) = c_{n+1} + c_{n} \)

From a) % chktex 10

\( c_{n} = cF_{n-1} + (1+c)F_{n} \)

\( b_{n} = -c + cF_{n-1} + (1+c)F_{n} = -c + c(F_{n-1}+F_{n}) + F_{n} = -c + F_{n} + cF_{n+1} \)

\section*{a) version 2} % chktex 9 % chktex 10

\( A(z) = \sum_{n \geq 0} a_{n} z^{n} = r + sz + \sum_{n \geq 2} a_{n} z^{n} = r + sz + \sum_{n \geq 0} a_{n+2}z^{n+2} = \)

\( = r + sz + \sum_{n \geq 0} (a_{n+1} + a_{n}) z^{n+2} = r + sz + z \sum_{n \geq 0} a_{n+1} z^{n+1} + z^{2} \sum_{n \geq 0} a_{n} z^{n} = \)

\( = r + sz + z \sum_{n \geq 1}a_{n} z^{n} + z^{2} A(z) = r + sz + z (A(z)-r) + z^{2} A(z) = \)

\( = r + (s-r)z + A(z)(z+z^{2}) \)

\( A(z) = \frac{r + (s-r)z}{1-z-z^{2}} \)

\( F(z) = \sum_{n \geq 0} F_{n}z^{n} = \frac{z}{1-z-z^{2}} \)

\( A(z) = \frac{r+(s-r)z}{z}F(z) = (s-r)F(z) + r \frac{1}{z}F(z) \)

\( a_{n} = (s-r)F_{n} + r F_{n+1} = s F_{n} + r(F_{n+1} - F_{n}) = s F_{n} + r F_{n-1} \)

\section*{b) version 2} % chktex 9 % chktex 10

\( B(z) = \sum_{z \geq 0} b_{n}z^{n} = z + \sum_{n \geq 2} b_{n+2}z^{n+2} = z + \sum_{n \geq 0} (b_{n+1} + b_{n} + c)z^{n+2} = \)

\( = z + z\sum_{n \geq 0} b_{n+1}z^{n+1} + z^{2} \sum_{n\geq 0}b_{n}z^{n} + \frac{cz^{2}}{1-z} = z + z(B(z) - 0) + z^{2} B(z) + \frac{cz^{2}}{1-z} \)

\( B(z) = \frac{z + \frac{cz^{2}}{1-z}}{1-z-z^{2}} = F(z)(1 + \frac{cz}{1-z}) = F(z) + F(z) z\frac{1}{1-z} \)

\( b_{n} = F_{n} + c[z^{n}]F(z)z\frac{1}{1-z} = F_{n} + c[z^{n-1}]F(z)\frac{1}{1-z} = F_{n} + c\sum_{k=0}^{n-1}F_{k} \)

\( F_{k} = F_{k+2} - F_{k+1} \)

\( \sum_{k=0}^{n-1}F_{k} = \sum_{k=0}^{n-1}(F_{k+2} - F_{k+1}) = F_{n+1} - F_{1} = F_{n+1} - 1 \)

\( b_{n} = F_{n} + c(F_{n+1} - 1) = -c + F_{n} + cF_{n+1} \)

\section*{a) version 3} % chktex 9 % chktex 10

\( x_{0} = 0, x_{1} = 1, x_{n+2} = x_{n+1} + x_{n} \implies x_{n} = F_{n} \)

\( x_{1} = 1, x_{1} = 0, x_{n+2} = x_{n+1} + x_{n} \implies x_{n} = F_{n-1} \)

\( a_{n} = rF_{n-1} + sF_{n} \)


\end{document} % chktex 17
