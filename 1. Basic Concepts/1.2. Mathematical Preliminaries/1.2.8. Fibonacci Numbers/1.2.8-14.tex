\documentclass{article}
\usepackage{amsmath, amssymb}

\title{1.2.8-14}

\begin{document}
\maketitle

\( A(z) := \sum_{n \geq 0}a_nz^n = z + \sum_{n \geq 0}(a_{n+1} + a_{n} + \binom{n}{m})z^{n+2} = \)

\( = z + z(A(z) - 0) + z^2 A(z) + \sum_{n \geq 0}\binom{n}{m} z^{n+2} = \)

\( = z + A(z)(z+z^2) + \sum_{n \geq -m}\binom{n+m}{m} z^{n+m+2} = \)

\( = z + A(z)(z+z^2) + \sum_{n \geq 0}\binom{n+m}{m} z^{n+m+2} = z+A(z)(z+z^2)+z^{m+2}\frac{1}{(1-z)^{m+1}} \)

\( A(z) = \frac{z + \frac{z^{m+2}}{(1-z)^{m+1}}}{1-z-z^2} = \frac{z}{1-z-z^2} + \frac{z}{1-z-z^2} \cdot \frac{z^{m+1}}{(1-z)^{m+1}} = F(z) + z^{m+1} \frac{1}{(1-z)^{m+1}} F(z) \)

\( F(z) = \frac{z}{1-z-z^2} \)

\( a_n = [z^n]A(z) = F_n + [z^{n-m-1}] \frac{1}{(1-z)^{m+1}}F(z) = F_n + \sum_{k=0}^{n-m-1} \binom{k+m}{m}F_{n-m-1-k} \)

\section*{version 2)}

\( b_n := \sum_{k=0}^m \binom{m+n-k}{k} \)

\( b_{n+2} - b_{n+1} = \sum_{k=0}^m (\binom{m+n+2-k}{k} - \binom{m+n+1-k}{k}) = \sum_{k=0}^m \binom{m+n-(k-1)}{k-1} = \sum_{k=-1}^{m-1}\binom{m+n-k}{k} = \sum_{k=0}^{m-1}\binom{m+n-k}{k} \)

\( b_{n+2} - b_{n+1} - b_n = -\binom{n}{m} \)

\( c_n := a_n + b_n \)

\( c_{n+2} = c_{n+1} + c_n \)

\( c_0 = a_0 + b_0 = 0 + \sum_{k=0}^m\binom{m-k}{k} \overset{1.2.8-16}{=} F_{m+1} = F_0 + F_{0+m+1} \)

\( c_1 = a_1 + b_1 = 1 + \sum_{k=0}^m\binom{m+1-k}{k} = 1 + \sum_{k=0}^{m+1}\binom{m+1-k}{k} - \binom{0}{m+1} \overset{1.2.8-16}{=} 1 + F_{m+2} = F_1 + F_{1 + m + 1} \)

\( c_n = F_n + F_{n+m+1} \)

\( a_n = F_n + F_{n+m+1} - \sum_{k=0}^m \binom{m+n-k}{k} \)


\end{document}
