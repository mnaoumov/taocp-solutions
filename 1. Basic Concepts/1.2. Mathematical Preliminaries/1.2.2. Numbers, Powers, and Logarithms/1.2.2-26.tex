\documentclass{article}
\input{../../../template}

\title{1.2.2-26} % chktex 8

\begin{document}
\maketitle

\( |\log_b{x_{0}} - y| \leq C_{L4} \cdot \frac{1}{2^{p}}(1 + \frac{1}{|\ln b|}) \)

If \( a = 2^{p} + 2^{q} + r, r < 2^{q} \)

Then after executing step L4, \( q \)-th bit becomes 0.

Therefore, \( C_{L4} \leq \) \[amount of non-leading bits in \( a_{0} \) \] \( \leq p \)

\( |\log_b{x_{0}} - y| \leq \frac{p}{2^{p}}(1 + \frac{1}{|\ln b|}) \)


\end{document}
