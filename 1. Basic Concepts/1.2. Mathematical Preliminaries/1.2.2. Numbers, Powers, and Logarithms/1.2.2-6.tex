\documentclass{article}
\input{../../../template} % chktex 27

\title{1.2.2-6} % chktex 8

\begin{document}
\maketitle

\( m = n, \forall k \geq 1: d_{k} = e_{k} \implies x = y \)

\( m < n \) or \( m = n, \exists k \geq 1 \forall j, 1 \leq j < k: d_{j} = e_{j}, d_{k} < e_{k} \implies x < y \)

\( m > n \) or \( m = n, \exists k \geq 1 \forall j, 1 \leq j < k: d_{j} = e_{j}, d_{k} > e_{k} \implies x > y \)


\end{document}
