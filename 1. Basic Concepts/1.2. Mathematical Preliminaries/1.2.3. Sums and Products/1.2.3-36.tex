\documentclass{article}
\input{../../../template}

\title{TAOCP 1.2.3\mbox{-}36}

\begin{document}
\maketitle

\( C = C(n,x,y) = \left(\begin{matrix}x + y & y & y & \cdots & y \\ y & x + y & y & \cdots & y \\ y & y & x + y & \cdots & y \\ \vdots & \vdots & \vdots & \ddots & \vdots \\ y & y & y & \cdots & x + y \end{matrix}\right) \)

\( |C| \overset{\substack{C_{2} \leftarrow C_{2} - C_{1} \\ C_{3} \leftarrow C_{3} - C_{1} \\ \vdots \\ C_{n} \leftarrow C_{n} - C_{1}}}{=}  \left|\begin{matrix}x + y & -x & -x & \cdots & -x \\ y & x & 0 & \cdots & 0 \\ y & 0 & x & \cdots & 0\\ \vdots & \vdots & \vdots & \ddots & \vdots \\ y & 0 & 0 & \cdots & x \end{matrix}\right| \overset{R_{1} \leftarrow R_{1} + R_{2} + \cdots + R_{n}}{=} \left|\begin{matrix}x + ny & 0 & 0 & \cdots & 0 \\ y & x & 0 & \cdots & 0 \\ y & 0 & x & \cdots & 0\\ \vdots & \vdots & \vdots & \ddots & \vdots \\ y & 0 & 0 & \cdots & x \end{matrix}\right| \)

It's triangular determinant

\( |C| = (x+ny)\cdot x \cdot x \cdot \cdots \cdot x = x^{n-1}(x+ny) \)


\end{document}
