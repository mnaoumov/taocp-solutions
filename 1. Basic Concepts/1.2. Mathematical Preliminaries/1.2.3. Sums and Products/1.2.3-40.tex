\documentclass{article}
\input{../../../template}

\title{1.2.3-40}

\begin{document}
\maketitle

\( V = V(x_1, x_2, \ldots x_n) = \left(\begin{matrix} x_1 & x_2 & \cdots & x_n \\ x_1^2 & x_2^2 & \cdots & x_n^2 \\ \vdots & \vdots & \ddots & \vdots \\ x_1^n & x_2^n & \cdots & x_n^n \end{matrix}\right) \)

\( \forall v: vV = (v_1x_1+v_2x_1^2+\cdots+v_nx_1^n,\ldots, v_1x_n+v_2x_n^2+\cdots+v_nx_n^n) = (p_v(x_1),p_v(x_2),\ldots,p_v(x_n)) \)

Where \( p_v(t) = v_1t + v_2t^2 + \cdots + v_nt^n \)

\( (p_v(x_1),p_v(x_2),\ldots,p_v(x_n))V^{-1} = v^T \)

\( \forall w \), let's find \( v: (p_v(x_1),p_v(x_2),\ldots,p_v(x_n)) = w^T \)

Observe that \( p_v(0) = 0 \)

\( x_0 := 0, w_0 := 0 \)

\( p_v(x_i) = w_i, 0 \leq i \leq n \)

\( q(t) := (t-x_0)(t-x_1)\cdots(t-x_n) \)

\( q_j(t) = \frac{q(t)}{t-x_j} \)

\( q_j(x_k) = 0, j \neq k \)

\( q_j(x_j) = q'(x_j) \neq 0 \)

\( l_j(t) = \frac{q_j(t)}{q_j(x_j)} \)

\( l_j(x_j) = 1, l_j(x_k) = 0, k \neq j \)

\( l_j(t) = \sum_{i=0}^n l_{ij}t^i \)

\( p_v(t) = \sum_{j=0}^n w_j l_j(t) \) - satisfies the conditions

\( p_v(t) = \sum_{j=0}^n\sum_{i=0}^n w_j l_{ij}t^i = \sum_{i=0}^n t^i \sum_{j=0}^n(l_{ij} w_j) \)

\( L := (l_{ij}) \)

\( p_v(t) = \sum_{i=0}^n t^i (Lw)_i \)

\( \implies p_v = p_{Lw} \)

\( \implies v = Lw \)

\( w^T L^T = v^T \implies V^{-1} = L^T \)

\( (V^{-1})_{ij} = (L^T)_{ij} = (L){ji} = l_{ji} = [t^j] l_i(t) \)

\( l_i(t) = \frac{1}{q_j(x_j)} (t-x_0)(t-x_1) \cdot\ \cdots\ \cdot \widehat{(x-x_j)} \cdot\ \cdots\ \cdot (x-x_n) = \frac{t}{q_j(x_j)} (t-x_1) \cdot\ \cdots\ \cdot \widehat{(x-x_j)} \cdot\ \cdots\ \cdot (x-x_n) = \)

\( = \frac{t}{q_j(x_j)} (t^n - e_1(x_1,\ldots,\widehat{x_j},\ldots, x_n)t^{n-1} + e_2(x_1,\ldots,\widehat{x_j},\ldots, x_n)t^{n-2} + \cdots + (-1)^n e_n(x_1,\ldots,\widehat{x_j},\ldots, x_n)) \)

\( (V^{-1})_{ij} = [t^j] l_i(t) = \frac{(-1)^{n+1-j}e_{n+1-j}(x_1,\ldots,\widehat{x_i},\ldots, x_n)}{q_j(x_j)} \)


\end{document}
