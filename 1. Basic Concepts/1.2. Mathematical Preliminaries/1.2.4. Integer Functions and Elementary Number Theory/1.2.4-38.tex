\documentclass{article}
\input{../../../template}

\title{TAOCP 1.2.4\mbox{-}38}

\begin{document}
\maketitle

\( A = \sum_{0 \leq k < y} \lfloor x + \frac{k}{y} \rfloor = \sum_{0 \leq k < \lceil y \rceil} \lfloor \lfloor x \rfloor + \{ x \} + \frac{k}{y} \rfloor = \sum_{0 \leq k < \lceil y \rceil} (\lfloor x \rfloor + \lfloor \{ x \} + \frac{k}{y} \rfloor) = \lfloor x \rfloor \cdot \lceil y \rceil + \sum_{0 \leq k < \lceil y \rceil} \lfloor \{ x \} + \frac{k}{y} \rfloor \)

\( 0 \leq \{ x \} + \frac{k}{y} \leq \{x \} + \frac{\lceil y \rceil - 1}{y} < 1 + 1 = 2 \)

We need to count summands that are \( \geq 1 \)

\( \leq \{ x \} + \frac{k}{y} \geq 1 \implies \{ x \} y + k \geq y \implies k \geq y(1-\{ x\}) \iff k \geq \lceil y(1-\{ x\}) \rceil =: k_{0} \)

There are \( \lceil y \rceil - k_{0} \) such \( k \).

\( A = \lfloor x \rfloor \cdot \lceil y \rceil + \lceil y \rceil - k_{0} = (\lfloor x \rfloor + 1) \lceil y\rceil - k_{0} = (\lfloor x \rfloor + 1) \lceil y\rceil + \lfloor -y(1-\{x\}) \rfloor = \)

\( = \lfloor (\lfloor x \rfloor + 1) \lceil y\rceil - y + y\{ x \} \rfloor = \lfloor (\lfloor x \rfloor + 1) \lceil y\rceil - y + yx - y\lfloor x \rfloor \rfloor = \)

\( = \lfloor xy + (\lfloor x \rfloor + 1)(\lceil y \rceil - y) \rfloor \)


\end{document}
