\documentclass{article}
\input{../../../template}

\title{1.2.4\mbox{-}34}

\begin{document}
\maketitle

Let \( n = \lfloor \log_b{\lfloor x \rfloor}\rfloor \)

\( n \leq \log_b{\lfloor x \rfloor} < n + 1 \)

\( b^{n} \leq \lfloor x \rfloor < b^{n+1} \)

\( \lfloor b^{n} \rfloor \leq \lfloor x \rfloor < \lceil b^{n+1} \rceil \)

\( \lfloor b^{n} \rfloor \leq x < \lceil b^{n+1} \rceil \)

\( \log_{b} \lfloor b^{n} \rfloor \leq \log_b{x} < \log_{b}\lceil b^{n+1} \rceil \)

\( \lfloor \log_{b} \lfloor b^{n} \rfloor \rfloor \leq \log_b{x} < \lceil \log_{b}\lceil b^{n+1} \rceil \rceil \)

\( \lfloor \log_{b} \lfloor b^{n} \rfloor \rfloor \leq \lfloor \log_b{x} \rfloor < \lceil \log_{b}\lceil b^{n+1} \rceil \rceil \)

\( \lfloor \log_{b} \lfloor b^{n} \rfloor \rfloor \leq \lfloor \log_{b}  b^{n} \rfloor = n < n + 1 = \lceil \log_{b} b^{n+1} \rceil \leq \lceil \log_{b}\lceil b^{n+1} \rceil \rceil \)

\( n = \lfloor \log_b{x} \rfloor \iff \lceil \log_{b}\lceil b^{n+1} \rceil \rceil -\lfloor \log_{b} \lfloor b^{n} \rfloor \rfloor = 1 \)

Consider \( n = 1 \)

\( \forall b \geq 2, b \in \mathbb{N} \) the equality holds.

If \( b \notin \mathbb{N} \)

\( \lfloor b \rfloor < b \)

\( \log_b{\lfloor b \rfloor} < 1 \)

\( \lfloor\log_b{\lfloor b \rfloor}\rfloor < 1 \implies \lfloor\log_b{\lfloor b \rfloor}\rfloor = 0 \)

\( \lfloor \log_{b} b \rfloor = 1 \)

\( \implies \) only for \( b \geq 2, b \in \mathbb{N} \)


\end{document}
