\documentclass{article}
\input{../../../template}

\title{TAOCP 1.2.4\mbox{-}47}

\begin{document}
\maketitle

\( p > 2 \) --- prime.

\( p = 2 w + 1 \)

\( b_{k} := \lfloor{\frac{2kq}{p}}\rfloor \)

\( a_{k} := ({(-1)}^{b_{k}} 2kq) \mod p \)

\section*{(a)}

\( \lnot(p \backslash q) \)

\( B := \{ 2, 4, \ldots, 2w \} \)

\( A := \{ a_{1}, a_{2}, \ldots a_{w} \} \)

Let \( 2kq = sp + t, 0 \leq t \leq p - 1 \)

\( a_{k} \equiv {(-1)}^{s}(sp+t) \equiv {(-1)}^{s} t \ (\mathrm{modulo}\ p) \)

\subsubsection*{1}

\( 2 \backslash s \implies s = 2u \)

\( a_{k} \equiv {(-1)}^{2u} t = t \ (\mathrm{modulo}\ p) \)

\( t = 2kq - 2up \equiv 0 \ (\mathrm{modulo}\ 2) \)

\( t = 2v, 0 \leq v \leq \frac{p-1}{2} = w \)

\( a_{k} = 2v \in B \)

\subsubsection*{2}

\( \lnot(2 \backslash s) \implies s = 2u + 1 \)

\( a_{k} \equiv {(-1)}^{2u+1} t = -t \equiv p - t \ (\mathrm{modulo}\ p) \)

\( t = 2kq - (2u+1)p \equiv -p \equiv 1 \ (\mathrm{modulo}\ 2) \)

\( t = 2v + 1, 0 \leq v \leq \frac{p-3}{2} = w - 1 \)

\( p - t = 2 (w - v) \)

\( a_{k} = 2(w-v) \in B \)

\bigskip\hrule\bigskip

\( \implies \forall k, 1 \leq k \leq w: a_{k} \in B \implies A \subset B \)

Let \( 1 \leq k < l \leq w \)

Assume \( a_{k} = a_{l} \)

\( {(-1)}^{b_{k}} 2kq \equiv {(-1)}^{b_{l}} 2lq \ (\mathrm{modulo}\ p) \)

\( -1 \equiv p - 1  \ (\mathrm{modulo}\ p) \)

\( (p - 1) \perp p, 2 \perp p, q \perp p \)

\( \implies ({(-1)}^{b_{l}} 2q) \perp p \)

\( l \equiv {(-1)}^{b_{k}-b_{l}}k \ (\mathrm{modulo}\ p) \)

\subsubsection*{1}

If \( 2 \backslash (b_{k} - b_{l}) \)

\( l \equiv k \ (\mathrm{modulo}\ p \implies l = k \) --- contradiction

\subsubsection*{2}

If \( \lnot(2 \backslash (b_{k} - b_{l})) \)

\( l \equiv -k \ (\mathrm{modulo}\ p \implies l = p - k \geq (2w+1)-w = w + 1 \) --- contradiction

\bigskip\hrule\bigskip

\( \implies a_{k} \neq a_{l} \implies \#A = w \implies A = B \)

\( a_{1}a_{2}\cdots a_{w} \equiv {(-1)}^{b_{1}+b_{2}+\cdots + b_{w}} (2q)(4q) \cdot\ \cdots\ \cdot (2wq) = {(-1)}^{\sigma} (2 \cdot 4 \cdot\ \cdots\ \cdot (2w))q^{w} \ (\mathrm{modulo}\ p) \)

\( A = B \implies a_{1}a_{2} \cdot\ \cdots\ \cdot a_{w} = 2 \cdot 4 \cdot\ \cdots\ \cdot (2w) \perp p \)

\( \implies (\frac{q}{p}) = q^{w} = {(-1)}^\sigma \)

\section*{(b)}

\( (\frac{2}{p}) = {(-1)}^\sigma \)

\( \sigma = \sum_{k=1}^{w} b_{k} \)

\( b_{k} = \lfloor{\frac{4k}{p}}\rfloor \)

\subsubsection*{1}

\( w = 2z \implies p = 2w + 1 = 4z + 1 \)

\( \forall k, 1 \leq k \leq z:  4k \leq 4z < 4z + 1 \)

\( b_{k} = \lfloor{\frac{4k}{4z+1}}\rfloor = 0 \)

\( \forall k, z+1 \leq k \leq 2z = w: 4z + 1 \leq 4(z + 1) \leq 4k \leq 4\cdot2z = 8z = (4z + 1) + (4z-1) \)

\( b_{k} = \lfloor{\frac{4k}{4z+1}}\rfloor = 1 \)

\( \implies \sigma = z = \lfloor{\frac{p+1}{4}}\rfloor \)

\subsubsection*{2}

\( w = 2z+1 \implies p = 2w + 1 = 4z + 3 \)

\( \forall k, 1 \leq k \leq z:  4k \leq 4z < 4z + 3 \)

\( b_{k} = \lfloor{\frac{4k}{4z+1}}\rfloor = 0 \)

\( \forall k, z+1 \leq k \leq 2z + 1 = w: 4z + 3 \leq 4(z + 1) \leq 4k \leq 4\cdot(2z + 1) = 8z + 4 = (4z + 3) + (4z+1) \)

\( b_{k} = \lfloor{\frac{4k}{4z+1}}\rfloor = 1 \)

\( \implies \sigma = z + 1 = \lfloor{\frac{p+1}{4}}\rfloor \)

\bigskip\hrule\bigskip

\( (\frac{2}{p}) = {(-1)}^{\lfloor{\frac{p+1}{4}}\rfloor} \)

Let \( p = 8s + t, t = 1,3,5,7 \)

\( \lfloor \frac{p+1}{4}\rfloor = 2s + \lfloor{\frac{t+1}{4}} \rfloor \)

\( (\frac{2}{p}) = {(-1)}^{2s + \lfloor{\frac{t+1}{4}} \rfloor} = {(-1)}^{\lfloor{\frac{t+1}{4}} \rfloor} \)

\( t = 1: \frac{t+1}{4} = 0 \)

\( t = 3: \frac{t+1}{4} = 1 \)

\( t = 5: \frac{t+1}{4} = 1 \)

\( t = 7: \frac{t+1}{4} = 2 \)

\( (\frac{2}{p})= \left\{\begin{matrix}1, & t=1,7\\-1, &t=3,5\end{matrix}\right. \)

\section*{(c)}

\( q > 2, \lnot(p \backslash q) \)

\( \sigma = \sum_{k=1}^{w} b_{k} \)

\( w = 2z + v, v = 0,1 \)

\( \forall k, 1 \leq k \leq z + v: b_{w+1-k}= \lfloor{\frac{2q(w+1-k)}{p}}\rfloor = \lfloor{\frac{q(2w+2-2k)}{p}}\rfloor = \lfloor{\frac{q(p+1-2k)}{p}}\rfloor = \)

\( = \lfloor{q - \frac{q(2k-1)}{p}}\rfloor = q - \lceil\frac{q(2k-1)}{p}\rceil \)

\( q \perp p, (2k - 1) \perp p \implies \frac{q(2k-1)}{p} \notin \mathbb{Z} \implies \lceil\frac{q(2k-1)}{p}\rceil = \lfloor\frac{q(2k-1)}{p}\rfloor + 1 \)

\( \implies b_{w+1-k} = (q - 1) - 2\lfloor\frac{q(2k-1)}{p}\rfloor + \lfloor\frac{q(2k-1)}{p}\rfloor \equiv \lfloor\frac{q(2k-1)}{p} \rfloor \ (\mathrm{modulo}\ 2) \)

\( \sigma = \sum_{k=1}^{z} b_{k} + \sum_{k=1}^{z} b_{w+1-k} + b_{z+1}[v=1] = \sum_{k=1}^{z}(b_{k}+b_{w+1-k}) + b_{z+1}[v=1] \equiv \)

\( \equiv \sum_{k=1}^{z}(\lfloor\frac{q(2k-1)}{p}\rfloor + \lfloor\frac{q\cdot 2k}{p}\rfloor) + b_{z+1}[v=1] = \sum_{j=1}^{2z} \lfloor\frac{qj}{p}\rfloor + b_{z+1}[v=1] \ (\mathrm{modulo}\ 2) \)

If \( v = 1 \implies w = 2z + 1 \implies b_{z+1} = b_{w+1-(z+1)} \equiv \lfloor{\frac{q(2(z+1)-1)}{p}}\rfloor = \lfloor{\frac{q(2z+1)}{p}}\rfloor \ (\mathrm{modulo}\ 2) \)

\( \implies \sigma \equiv \sum_{j=1}^{2z} \lfloor\frac{qj}{p}\rfloor + \lfloor{\frac{q(2z+1)}{p}}\rfloor[v=1] = \sum_{j=1}^{w} \lfloor\frac{qj}{p}\rfloor = \sum_{1 \leq j < \frac{p}{2}} \lfloor\frac{qj}{p}\rfloor = \sum_{0 \leq j < \frac{p}{2}} \lfloor\frac{qj}{p}\rfloor \)

\section*{(d)}

\( (\frac{q}{p}) = {(-1)}^{\overbrace{\sum_{0 \leq j < \frac{p}{2}} \lfloor\frac{qj}{p}\rfloor}^{A}} \)

\( (\frac{p}{q}) = {(-1)}^{\overbrace{\sum_{0 \leq j < \frac{q}{2}} \lfloor\frac{pj}{q}\rfloor}^{B}} \)

1.2.4-46 Let \( \alpha = \frac{1}{2}, n = p, m = q, f(x) = x \) % chktex 8

\( \implies A = \sum_{0 \leq r < \frac{q}{2}}{\lceil\frac{pr}{q}\rceil} (r - 1 -r) + \lceil{\frac{p}{2}}\rceil(\lceil{\frac{q}{2}}\rceil - 1) \)

\( \lceil{\frac{p}{2}}\rceil = \frac{p+1}{2} \)

\( \lceil{\frac{q}{2}}\rceil = \frac{q+1}{2} \)

\( r > 0 \implies \lceil\frac{pr}{q}\rceil = 1 + \lfloor\frac{pr}{q}\rfloor \)

\( A = 0 - \sum_{1\leq r < \frac{q}{2}} (1 + \lfloor\frac{pr}{q}\rfloor) + \frac{(p+1)(q-1)}{4} = -B - \lfloor{\frac{q}{2}}\rfloor + \frac{(p+1)(q-1)}{4} = \)

\( = -B - \frac{q-1}{2} + \frac{(p+1)(q-1)}{4} = -B + \frac{q-1}{2}(-1 + \frac{p+1}{2}) = - B + \frac{(p-1)(q-1)}{4} \)

\( (\frac{q}{p})(\frac{p}{q}) = {(-1)}^{A+B} = {(-1)}^{\frac{(p-1)(q-1)}{4}} \)


\end{document} % chktex 17
