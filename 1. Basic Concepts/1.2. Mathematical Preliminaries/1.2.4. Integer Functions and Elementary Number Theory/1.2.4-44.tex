\documentclass{article}
\input{../../../template}

\title{TAOCP 1.2.4\mbox{-}44}

\begin{document}
\maketitle

\begin{multline*}
\sum_{k \geq 0}\sum_{1 \leq j < b} \lfloor{\frac{n + jb^{k}}{b^{k+1}}}\rfloor = \sum_{k \geq 0}(\sum_{0 \leq j < b} \lfloor{\frac{n}{b^{k+1}} + \frac{j}{b}}\rfloor - \lfloor{\frac{n}{b^{k+1}}}\rfloor) = \\
\sum_{k \geq 0}(\lfloor{b \cdot \frac{n}{b^{k+1}}}\rfloor - \lfloor{\frac{n}{b^{k+1}}}\rfloor) =
\end{multline*}

\begin{multline*}
= \sum_{k \geq 0}(\lfloor{\frac{n}{b^{k}}}\rfloor - \lfloor{\frac{n}{b^{k+1}}}\rfloor) = \lim_{K \to \infty} \sum_{k = 0}^{K}(\lfloor{\frac{n}{b^{k}}}\rfloor - \lfloor{\frac{n}{b^{k+1}}}\rfloor) = \\
\lim_{K \to \infty}(\lfloor{\frac{n}{b^{0}}}\rfloor) - (\lfloor{\frac{n}{b^{K+1}}}\rfloor) =
\end{multline*}

\( = n - \lim_{K \to \infty} (\lfloor{\frac{n}{b^{K+1}}}\rfloor) =: A \)

If \( n \geq 0 \implies 0 \leq \lfloor{\frac{n}{b^{K+1}}}\rfloor \leq \frac{n}{b^{K + 1}} \to 0, K \to \infty \implies A = n \)

If \( n < 0 \):
\[ \frac{n}{b^{K+1}} - 1 <\lfloor{\frac{n}{b^{K+1}}}\rfloor \leq \frac{n}{b^{K+1}} < 0 \implies \frac{n}{b^{K+1}} - 1 <\lfloor{\frac{n}{b^{K+1}}}\rfloor \leq -1 \]

\( \frac{n}{b^{K+1}} \to 0, K \to \infty \implies \lim_{K \to \infty} (\lfloor{\frac{n}{b^{K+1}}}\rfloor) = -1 \implies A = n + 1 \)


\end{document}
