\documentclass{article}
\input{../../../template}

\title{TAOCP 1.2.6\mbox{-}27}

\begin{document}
\maketitle

\( \sum_{k} A_{k}(r,t)z^{k} = x^{r} \)

\( \sum_{k} A_{k}(s,t)z^{k} = x^{s} \)

\( \sum_{n} A_{n}(r+s,t)z^{k} = x^{r+s} \)

\( x^{r+s} = x^{r} x^{s} = \sum_{n} x^{n} \sum_{k} A_{k}(r,t)A_{n-k}(s,t) \)

\( A_{n}(r+s,t) = \sum_{k} A_{k}(r,t)A_{n-k}(s,t) \)

\( \sum_{k} A_{k}(r,t)z^{k} = x^{r} \)

\( \sum_{k} \binom{s-tk}{k}z^{k} = \frac{x^{s+1}}{(t+1)x-t} \)

\( \sum_{n} z^{n} (\sum_{k} A_{k}(r,t) \binom{s-t(n-k)}{n-k}) = \frac{x^{r+s+1}}{(t+1)x - t} = \sum_{n}\binom{r+s-tn}{n}z^{n} \)

\( \sum_{k} A_{k}(r,t) \binom{s-t(n-k)}{n-k} = \binom{r+s-tn}{n} \)


\end{document}
