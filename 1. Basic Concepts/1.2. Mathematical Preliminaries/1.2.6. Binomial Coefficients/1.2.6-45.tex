\documentclass{article}
\input{../../../template}

\title{TAOCP 1.2.6\mbox{-}45}

\begin{document}
\maketitle

\( A := \lim_{r \to \infty} \overbrace{\frac{\binom{r}{k}}{r^{k}}}^{A_{r}} \)

\( A_{r} = \frac{\Gamma(r+1)}{\Gamma(k+1)\Gamma(r-k+1) r^{k}} = \frac{1}{\Gamma(k+1)} \cdot \frac{\sqrt{2 \pi r} {\left(\frac{r}{e}\right)}^{r} (1 + o(1))}{\sqrt{2 \pi (r-k)} {\left(\frac{r-k}{e}\right)}^{r-k} r^{k} (1 + o(1))} = \)

\( = \frac{1}{\Gamma(k+1)} \cdot \sqrt{\frac{r}{r-k}} {\left(\frac{r}{r-k}\right)}^{r-k} e^{-k} (1+o(1)) = \frac{e^{-k}}{\Gamma(k+1)} {\left({\left(1 + \frac{1}{\frac{r-k}{k}}\right)}^{\frac{r-k}{k}}\right)}^{k} (1+o(1)) \)

\( A = \frac{1}{\Gamma(k+1)} \)


\end{document}
