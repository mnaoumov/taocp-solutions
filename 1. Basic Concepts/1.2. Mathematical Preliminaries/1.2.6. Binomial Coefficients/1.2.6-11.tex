\documentclass{article}
\input{../../../template}

\title{TAOCP 1.2.6\mbox{-}11}

\begin{document}
\maketitle

\( \mu(x,p) = \sum_{k > 0}{\lfloor{\frac{x}{p^{k}}}\rfloor} \)

\( \binom{a+b}{a} = \frac{(a+b)!}{a!b!} \)

\( n = \mu(a+b,p) - \mu(a,p) - \mu(b, p) \)

Let \( a = \sum_{k \geq 0} a_{k} p^{k} \)

\( b = \sum_{k \geq 0} b_{k} p^{k} \)

\( a + b = \sum_{k \geq 0} c_{k} p^{k} \)

1.2.5-12 % chktex 8

\( \mu(a, p) = \frac{a - \sum_{k \geq 0} a_{k}}{p-1} \)

\( \mu(b, p) = \frac{b - \sum_{k \geq 0} b_{k}}{p-1} \)

\( \mu(a+b, p) = \frac{a+b - \sum_{k \geq 0} c_{k}}{p-1} \)

\( n = \mu(a+b,p) - \mu(a,p) - \mu(b, p) = \frac{\sum_{k \geq 0}(a_{k} + b_{k} - c_{k})}{p-1} \)

\( d_{0} := 0 \)

\( c_{0} = a_{0} + b_{0} + d_{0}, d_{1} = 0 \), if \( d_{0} + a_{0} + b_{0} < p \)

\( c_{0} = a_{0} + b_{0} + d_{0} - p, d_{1} = 1 \), if \( a_{0} + b_{0} + d_{0} \geq p \)

\( a_{0} + b_{0} - c_{0} = pd_{1} - d_{0} \)

\( c_{1} = a_{1} + b_{1} + d_{1}, d_{2} = 0 \), if \( a_{1} + b_{1} + d_{1} < p \)

\( c_{1} = a_{1} + b_{1} + d_{1} - p, d_{2} = 1 \), if \( a_{1} + b_{1} + d_{1} \geq p \)

\( a_{1} + b_{1} - c_{1} = pd_{2} - d_{1} \)

\( \vdots \)

\( a_{k} + b_{k} - c_{k} = pd_{k+1} - d_{k} \)

\( n = \frac{\sum_{k \geq 0}(pd_{k+1} - d_{k})}{p-1} = \frac{p \sum_{k \geq 1}d_{k} - \sum_{k \geq 0}d_{k}}{p - 1} = \frac{p \sum_{k \geq 0}d_{k} - \sum_{k \geq 0}d_{k}}{p - 1} = \sum_{k \geq 0}d_{k} = \# \{ \textrm{carries for adding}\ a+b \} \)


\end{document}
