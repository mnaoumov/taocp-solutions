\documentclass{article}
\input{../../../template}

\title{TAOCP 1.2.6\mbox{-}68}

\begin{document}
\maketitle

\( a_{k} := \binom{n}{k}p^{k}{(1-p)}^{n-k}(k-np) \)

\( S = \sum_{k} |a_{k}| = \sum_{k: k \geq np} a_{k} - \sum_{k: k < np} a_{k} = \sum_{k: k \geq \lceil np \rceil} a_{k} - \sum_{k: k < \lceil np \rceil} a_{k} = \sum_{k = \lceil np \rceil}^{n} a_{k} - \sum_{k = 0}^{\lceil np \rceil - 1} a_{k} \)

\( b_{k} := k \binom{n}{k}p^{k}{(1-p)}^{n+1-k} \)

\( b_{k} - b_{k+1} = k \binom{n}{k}p^{k}{(1-p)}^{n+1-k} - (k+1) \binom{n}{k+1}p^{k+1}{(1-p)}^{n+1-k-1} = \)

\( = p^{k}{(1-p)}^{n-k} \left(k\binom{n}{k}(1-p) - (k+1)\binom{n}{k+1}p\right) =: A \)

\( (k+1)\binom{n}{k+1} = (n-k)\binom{n}{k} \)

\( A = \binom{n}{k}p^{k}{(1-p)}^{n-k}(k(1-p) - (n-k)p) = \binom{n}{k}p^{k}{(1-p)}^{n-k}(k - np) = a_{k} \)

\( m := \lceil np \rceil \)

\( S = b_{m} - b_{n+1} - (b_{0} - b_{m}) = 2b_{m} - b_{n+1} - b_{0} \)

\( b_{0} = b_{n+1} = 0 \)

\( S = 2b_{m} \)


\end{document}
