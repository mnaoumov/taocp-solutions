\documentclass{article}
\input{../../../template} % chktex 27

\title{1.2.1-6} % chktex 8

\begin{document}
\maketitle

\[a'm+b'n = c \tag{1}\label{eq:1}\]

\[am + bn = d \tag{2}\label{eq:2}\]

Before \( E4 \): \( c \leftarrow d, d \leftarrow r, t \leftarrow a', a' \leftarrow a, a \leftarrow t - qa, t \leftarrow b', b' \leftarrow b, b \leftarrow t - qb \)

We need to check the equalities hold after \( E4 \).

For simplicity we will use index \( 1 \) for values after.

So we need to verify

\[a_{1}'m+b_{1}'n = c_{1} \tag{1'}\label{eq:1'}\]

\[a_{1}m + b_{1}n = d_{1} \tag{2'}\label{eq:2'}\]

Let's substitute new values using \( E4 \) assignments.

\( c_{1}=d, d_{1}=r, a'_{1}=a,a_{1}=a'-qa,b_{1}'=b,b_{1}=b'-qb \)

\( a_{1}'m+b_{1}'n=am+bn\overset{\eqref{eq:2}}{=}d=c' \implies~\eqref{eq:1'} \)

\( a_{1}m+b_{1}n=(a'-qa)m+(b'-qb)n=(a'm+b'n)-q(am+bn)\overset{\eqref{eq:1},~\eqref{eq:2}}{=}c-qd = r = d_{1} \implies~\eqref{eq:2'} \)


\end{document}
