\documentclass{article}
\input{../../../template}

\title{TAOCP 1.2.1\mbox{-}15}

\begin{document}
\maketitle

\subsubsection*{a)} % chktex 9 % chktex 10

iii) \( A = \mathbb{Z} \neq \varnothing \) % chktex 10

Assume \( \exists x \in A \forall y \in A: x \leq y \)

Take \( y = x - 1, y \in A, \) but \( x \not\leq y \)

Contradiction \( \implies (\mathbb{Z}, <) \) is not well-ordered.

\subsubsection*{b)} % chktex 9 % chktex 10

Let's define ordering as \( 0, -1, 1, -2, 2, -3, 3, \cdots \)

Namely, \( x \prec y \overset{def}{\iff} |x| < |y| \) or \( |x|=|y|, x < 0, y > 0 \)

i) \( \forall x, y, z \in \mathbb{Z}, x \prec y, y \prec z \) % chktex 10

\item \( |x|<|y|, |y|<|z| \implies |x|<|z| \implies x \prec z \)
\item \( |x|=|y|, x < 0, y > 0, |y| < |z| \implies |x| < |z| \implies x \prec z \)
\item \( |x|<|y|, |y| = |z|, y < 0, z > 0, \implies |x| < |z| \implies x \prec z \)
\item \( |x|=|y|, x < 0,  y > 0, |y| = |z|, y < 0, z > 0 \implies y > 0 \) and \( y < 0 \) --- contradiction

ii) \( \forall x, y \in \mathbb{Z} \) % chktex 10

\item \( |x|<|y| \implies x \prec y \)
\item \( |x|>|y| \implies y \prec x \)
\item \( |x|=|y|, x < 0, y < 0 \implies x = y \)
\item \( |x|=|y|, x < 0, y > 0 \implies x \prec y \)
\item \( |x|=|y|, x > 0, y < 0 \implies y \prec x \)
\item \( |x|=|y|, x > 0, y > 0 \implies x = y \)

iii) Let \( A \subset \mathbb{Z}, B \neq \varnothing, B = \{ |n|: n \in A \} \subset \mathbb{N}, B \neq \varnothing \) % chktex 10

\( \mathbb{N} \) --- well-ordered \( \implies \exists x \in B \forall y \in B: x \leq y \)

\( x \in B \implies -x \in A \) or \( x \in A \)

\item \( z := -x \in A. \forall y \in A: |y| \in B \implies x \leq |y| \implies |-x| \leq |y| \)
\item \( |-x| < |y| \implies -x \prec y \)
\item \( |-x|=|y|, -x < 0, y < 0 \implies x = y \)
\item \( |-x|=|y|, -x < 0, y > 0 \implies x \prec y \)
\item \( -x \notin A, z := x \in A. \forall y \in A: |y| \in B \implies x \leq |y| \implies |x| \leq |y| \)
\item \( |x| < |y| \implies x \prec y \)
\item \( |x|=|y|, x > 0, y > 0 \implies x = y \)
\item \( |x|=|y|, x > 0, y < 0 \implies y=-x \in S \) --- contradiction.

\( \implies \exists z \in A \forall y \in A: z \preceq y \)

\subsubsection*{c)} % chktex 9 % chktex 10

No. Let's \( A = \{ x \in \mathbb{R}: x > 0 \} \)

Assume \( \exists x \in A \forall y \in A: x \leq y \)

Take \( A = \frac{x}{2} \in A, y < x \implies x \not\leq y \) --- contradiction.

\subsubsection*{d)} % chktex 9 % chktex 10

\( \forall x, y, z \in T_{n} \)

\( x = (x_{1},x_{2},\ldots,x_{n}) \)

\( y = (y_{1},y_{2},\ldots,y_{n}) \)

\( z = (z_{1},z_{2},\ldots,z_{n}) \)

i) \( x \prec y, y \prec z \) % chktex 10

\( \exists k, 1 \leq k \leq n: x_{j} = y_{j}, 1 \leq j < k, x_{k} < y_{k} \)

\( \exists l, 1 \leq l \leq n: y_{j} = z_{j}, 1 \leq j < l, y_{l} < z_{l} \)

\item \( k = l \implies x_{j} = y_{j} = z_{j}, 1 \leq j < k, x_{k} < y_{k} = y_{l} < z_{l} = z_{k} \implies x \prec z \)
\item \( k < l \implies x_{j}=y_{j}=z_{j}, 1 \leq j < k, x_{k} < y_{k} = z_{k} \implies x \prec z \)
\item \( k > l \implies x_{j} = y_{j} = z_{j}, 1 \leq j < l, x_{l}=y_{l} < z_{l} \implies x \prec z \)

ii) \( \forall x, y \in T_{n} \) % chktex 10

\item \( \forall j, 1 \leq j \leq n: x_{j}=y_{j} \implies x=y \)
\item \( \exists k, 1 \leq k \leq n \forall j, 1 \leq j < k: x_{j} = y_{j}, x_{k} \neq y_{k} \)
\item \( x_{k} < y_{k} \implies x \prec y \)
\item \( x_{k} > y_{k} \implies y \prec x \)

iii) \( A \subset T_{n}, A \neq \varnothing \) % chktex 10

\( A_{0} := A \neq \varnothing \)

\( B_{1} := \{ x_{1}: x \in A_{0} \} \subset S, B_{1} \neq \varnothing \implies \exists a_{1} \in B_{1} \forall b_{1} \in B_{1}: a_{1} \leq b_{1} \)

\( A_{1} := \{ x \in A_{0}: x_{1} = a_{1} \} \)

\( a_{1} \in B_{1} \implies \exists x \in A_{0}: x_{1} = a_{1} \implies A_{1} \neq \varnothing \)

\( \cdots \)

\( \forall j, 1 \leq j \leq n \)

\( B_{j} := \{ x_{j}: x \in A_{j-1} \} \neq \varnothing \implies \exists a_{j} \in B_{j} \forall b_{j} \in B_{j}: a_{j} \leq b_{j} \)

\( A_{j} := \{ x \in A_{j-1}: x_{1} = a_{j} \} \)

\( \cdots \)

\( a := (a_{1}, a_{2}, \ldots, a_{n}) \)

\( A=A_{0} \supset A_{1} \supset A_{2} \supset \cdots \supset A_{n} \)

\( a \in A_{n} \implies a \in A \)

\( \forall b \in A \)

Assume \( b \prec a \implies \exists k, 1 \leq k \leq n \forall j, 1\leq j < k: b_{j} = a_{j}, b_{k} < a_{k} \)

\( b_{1} = a_{1} \implies b \in A_{1} \)

\( b_{2} = a_{2} \implies b \in A_{2} \)

\( b_{k-1} = a_{k-1} \implies b \in A_{k-1} \)

\( \implies B_{k} \ni b_{k} \implies a_{k} \leq b_{k} \) --- contradiction

\( \implies a \preceq b \)

\subsubsection*{e)} % chktex 9 % chktex 10

No, Let's \( S = \{ a, b \}, a \prec b \)

\( x^{(1)} := (b) \in T_{1} \)

\( x^{(2)} := (a, b) \in T_{2} \)

\( x^{(3)} := (a, a, b) \in T_{3} \)

\( \cdots \)

\( x^{(n)} := (a,a,\ldots,a,b) \in T_{n} \)

\( A := \{ x^{(1)}, x^{(2)}, \ldots \} \neq \varnothing \)

Assume \[\exists x \in A \forall y \in A: x \prec y \tag{1}\label{eq:1e}\]

\( x = x^{(n)} = (a,a,\ldots,a,b) \in T_{n} \)

Take \( y = x^{(n+1)} = (a,a,\ldots,a,a,b) \in T_{n+1} \)

\( \forall j, 1 \leq j < n: x_{j} = y_{j} = a, x_{n} = b, y_{n} = a \implies y \prec x \) --- contradicts to~\eqref{eq:1e}

\( \implies S \) is not well-ordered.

\subsubsection*{f)} % chktex 9 % chktex 10

\( (\implies) \) \( S \) satisfies (i)-(iii) % chktex 36

Assume exists sequence: \( \cdots \prec x_{3} \prec x_{2} \prec x_{1} \)

\( A := \{ x_{1}, x_{2}, x_{3}, \ldots \} \)

(iii) \( \implies \)
\[\exists x \in A \forall y \in A: x \prec y \tag{1}\label{eq:1f}\]

\( x = x_{n} \)

Take \( y = x_{n+1} \)

By definition: \( x_{n+1} \prec x_{n} \implies y \prec x \) --- contradicts to~\eqref{eq:1f}

\( \implies \) such sequence doesn't exist

\( (\Longleftarrow) \) \( S \) satisfies (i)-(ii) and doesn't exist sequence: \( \cdots \prec x_{3} \prec x_{2} \prec x_{1} \) % chktex 36

Assume (iii) doesn't hold \( \implies \) for some \( A \subset S, A \neq \varnothing, \nexists x \in A \forall y \in A: x \prec y \)

\( A \neq \varnothing \implies \exists x_{1} \in A \)

\( x_{1} \neq \min A \implies \exists x_{2} \in A: x_{2} \prec x_{1} \)

\( x_{2} \neq \min A \implies \exists x_{3} \in A: x_{3} \prec x_{2} \)

\( \cdots \)

Got sequence: \( \cdots \prec x_{3} \prec x_{2} \prec x_{1} \) --- contradiction

\( \implies \) (iii)

\subsubsection*{g)} % chktex 9 % chktex 10

Assume \( P(x) \) doesn't hold for all \( x \in S \).

\( A := \{ x \in S: P(x) \) --- does not hold \( \} \implies A \neq \varnothing \)

\( \exists x \in A \forall y \in A: x \prec y \)

\( \forall y \prec x \implies y \notin A \implies P(y) \implies P(x) \implies x \in A \) --- contradiction.


\end{document} % chktex 17
