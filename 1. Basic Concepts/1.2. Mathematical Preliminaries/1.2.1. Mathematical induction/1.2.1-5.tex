\documentclass{article}
\input{../../../template}

\title{1.2.1-5}

\begin{document}
\maketitle

\( P(n) \iff \{ n > 1, n\, \) is a product of prime numbers \( \} \)

\( P(1) \) holds automatically

Assume \( P(k) \) holds for all \( k < n \)

If \( n \) is prime, then \( n = n \) is a product of prime numbers and \( P(n) \) holds.

If \( n \) is not prime, then \( n = ab, a, b > 1 \implies a, b < n \implies P(a), P(b) \)

\( a = p_1p_2 \cdot\ \cdots\ \cdot p_k \)

\( b = q_1q_2 \cdot\ \cdots\ \cdot q_l \)

\( \implies n = ab = p_1p_2 \cdot\ \cdots\ \cdot p_k q_1q_2 \cdot\ \cdots\ \cdot q_l \) - product of primes \( \implies P(n) \)

\textbf{Q.E.D.}


\end{document}
